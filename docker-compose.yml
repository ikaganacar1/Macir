services:
  backend:
    build:
      context: ./backend
    env_file:
      - .env
    environment:
      DEBUG: "false"
      EXTRA_ALLOWED_ORIGINS: "https://macir.ikaganacar.com"
    volumes:
      - db_data:/app/db
      - media_data:/app/media
    expose:
      - "8000"

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    expose:
      - "80"

  nginx:
    image: nginx:alpine
    ports:
      - "25566:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - backend
      - frontend

volumes:
  db_data:
  media_data:
